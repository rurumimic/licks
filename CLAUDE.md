# licks

Axum-based web server.

## Development

When wrapping up a unit of Rust work, clippy must pass with no warnings:

```bash
cargo clippy --all-targets -- -D warnings
```

Treat any warning as a failure - fix it and re-run.
